[build]
  publish = "/"
  command = ""

# Ensure HTML files are served with correct headers
[[headers]]
  for = "/*"
  [headers.values]
    X-Frame-Options = "DENY"
    X-XSS-Protection = "1; mode=block"
