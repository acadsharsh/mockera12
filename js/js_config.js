const CONFIG = {
    // In production, this would be your deployed backend URL (e.g., https://api.myapp.com)
    // For local development, it points to the local Node.js server
    API_BASE_URL: 'http://localhost:3000/api',
};
